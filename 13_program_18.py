#Raise an error annd stop the program if x is lower than 0:

x=int(input())
if x<0:
    raise Exception("only,Legends can understand")

y="hello"
if not type(x) is int:
    raise TypeError("everyone be a legend now a days")
